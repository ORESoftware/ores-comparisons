# Realtime chat contracts

`main.tsp` and `entities.schema.json` are independent peer authorities. The storage manifest defines the Postgres projection and `projection.lock.json` freezes Protobuf wire identities.
