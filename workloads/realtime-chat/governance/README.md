# Realtime chat governance

All stacks share one message/conversation contract. `sequence` is a per-conversation ordering field and its meaning may not be silently changed by a runtime. Tenant isolation applies to both parent and child rows. Protobuf numbers are append-only; removed fields must be reserved before reuse is considered.
