# RPC / GraphQL governance

RPC, GraphQL, and HTTP are projections over the same entity authority. A transport-specific schema may narrow transport details but may not redefine persistence semantics. `revision` and event `sequence` are monotonic application contracts. Protobuf identities are locked independently from GraphQL field naming.
