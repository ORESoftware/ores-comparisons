# RPC / GraphQL contracts

The persisted entity contract is shared across REST/RPC/GraphQL projections. TypeSpec and JSON Schema are independent peer authorities; transport projections must not mutate the entity semantics.
