# Form service contracts

`main.tsp` and `entities.schema.json` are independent peer authorities. Run the repository `check-contracts.sh` gate before trusting downstream projections. `storage.manifest.json` is the explicit relational mapping; `projection.lock.json` freezes Protobuf field numbers and enum ordinals.
