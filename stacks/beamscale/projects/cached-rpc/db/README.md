# Local database

The local cluster admits contracts, generates and cross-checks SQL, migrates `rpc_methods` and `rpc_cache_entries`, seeds deterministic RPC/cache fixtures, then starts BeamScale dev.
