# BeamScale / cached RPC governance

RPC operation identity/cache semantics are shared contract data; the BeamScale worker remains capability-restricted. Postgres is a local comparison dependency managed outside tenant actor authority.
