# BeamScale RPC/GraphQL governance

BeamScale RPC actors bind to the same persistence/entity authority as the other stacks. RPC/GraphQL transport generation is downstream evidence; it may not redefine `RpcResource`, event sequence, or tenant semantics.
