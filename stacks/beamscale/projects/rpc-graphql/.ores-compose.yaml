schema_version: ores.compose.v1
project: beamscale-rpc-graphql
allow_lazy_start: false
services:
  postgres:
    runtime: host
    command: ["../../../../scripts/local-postgres.sh", "beamscale-rpc-graphql"]
    healthcheck:
      command: ["../../../../scripts/local-postgres-health.sh", "beamscale-rpc-graphql"]
      interval_ms: 1000
      timeout_ms: 1000
      retries: 20
  app:
    runtime: host
    command: ["../../../../scripts/dev-project.sh", "beamscale", "."]
    depends_on: ["postgres"]
