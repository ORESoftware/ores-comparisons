# Database

Local Postgres is started by `.ores-compose.yaml`. `scripts/dev-project.sh` regenerates the admitted SQL projection and applies it plus the canonical form-service seed before BeamScale dev starts.
