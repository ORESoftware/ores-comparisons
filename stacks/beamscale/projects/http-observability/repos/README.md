# Repositories

This project can contain multiple repositories. The current BeamScale application lives in `repos/app/`. Additional repos belong beside it. Project-level `.ores-compose.yaml`, contracts, conformance, governance, environment policy, and orchestration scripts stay above `repos/`.
