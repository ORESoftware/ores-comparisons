# BeamScale decrypted environments

Local-only material produced from `../enc/` with SOPS + age. This directory is ignored by git except for this documentation file. Load a decrypted file into `bmscl build`, `bmscl dev`, or `bmscl deploy` through the process environment; never pass bearer tokens or private keys as command-line flags.
