# Scintilla realtime-chat governance

Scintilla endpoint/container semantics remain separate from the shared chat data authority. The pinned local control plane is a dev dependency only; message ordering and persistence semantics come from the workload contract.
