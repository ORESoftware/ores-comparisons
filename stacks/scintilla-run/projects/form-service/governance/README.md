# Scintilla form-service governance

This project uses the shared form entity authority while Scintilla owns only endpoint/container/sub-process execution semantics. Local compose also launches the pinned Scintilla control plane at loopback for `scintilla dev` synchronization.
