# Scintilla / forms-chat governance

Container/sub-process execution may differ, but workflow/form/message persistence and ordering semantics are shared. The pinned local Scintilla control plane cannot redefine the workload contract.
