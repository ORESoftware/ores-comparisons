# Scintilla RPC/GraphQL governance

Endpoint/container definitions project the common resource/event authority. The local control plane is pinned separately and cannot redefine the TypeSpec/JSON Schema persistence contract.
