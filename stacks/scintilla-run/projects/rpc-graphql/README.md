# Scintilla rpc-graphql

Container/subprocess version of the matched contract workload. RPC and GraphQL are separate canonical endpoint slugs but share the same `api-docs` contract digest and integration environment.

```sh
scintilla build --project . --out-dir .scintilla
scintilla deploy --project . --out-dir .scintilla --dry-run
```
