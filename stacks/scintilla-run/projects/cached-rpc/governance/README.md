# Scintilla / cached RPC governance

Scintilla endpoint mechanics project the common RPC/cache authority. Cache state, operation identity, revisions, and persistence semantics remain shared across stacks.
