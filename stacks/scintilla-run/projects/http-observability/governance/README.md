# Scintilla / HTTP observability governance

Scintilla owns endpoint/container/sub-process execution only. The HTTP observation/span model comes from the shared peer-authority workload. Local endpoint sync waits for both Postgres readiness and the pinned loopback Scintilla control plane.
