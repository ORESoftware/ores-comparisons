# Scintilla encrypted environments

Commit only SOPS ciphertext here. Use age recipients supplied by the owning organization; do not store private keys in this repository. Typical files are `dev.env.yaml`, `stage.env.yaml`, and `prod.env.yaml`.
