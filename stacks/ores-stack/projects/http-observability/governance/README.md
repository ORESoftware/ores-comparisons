# ORES Stack / HTTP observability governance

Rust/WASM/server and api-docs projections consume the common request/span authority. Generated routing/RPC code may not replace the TypeSpec/JSON Schema entity contract.
