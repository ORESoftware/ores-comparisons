# ores-stack-big-org-example-commerce contract tests

Independent sibling repository that validates the organization's `.github`
JSON Schema authority against its valid/invalid fixture corpus.

```sh
python3 verify.py
```
