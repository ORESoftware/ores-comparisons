# ORES Stack / forms-chat governance

Rust/WASM/API surfaces are generated consumers of the common workflow/form/message authority. Runtime-specific routes and clients cannot redefine persistence or state-machine semantics.
