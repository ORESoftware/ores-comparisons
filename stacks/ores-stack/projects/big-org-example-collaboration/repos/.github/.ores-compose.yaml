schema_version: ores.compose.v1
project: compareoresbigcollab
allow_lazy_start: false
services:
  postgres:
    runtime: host
    command: ["bash", "scripts/postgres-local.sh"]
    healthcheck:
      command: ["bash", "scripts/db-health.sh"]
      interval_ms: 300
      timeout_ms: 250
      retries: 20
  db-bootstrap:
    runtime: host
    command: ["bash", "scripts/db-bootstrap.sh"]
    build:
      - ["bash", "scripts/contracts-check.sh"]
      - ["bash", "scripts/db-migrate.sh"]
      - ["bash", "scripts/db-seed.sh"]
    depends_on: [postgres]
    healthcheck:
      command: ["bash", "scripts/db-bootstrap-health.sh"]
      interval_ms: 200
      timeout_ms: 200
      retries: 20
  presence-service:
    runtime: host
    command: ["python3", "scripts/run-sibling.py", "run", "presence-service"]
    build:
      - ["python3", "scripts/run-sibling.py", "build", "presence-service"]
    depends_on: ["db-bootstrap"]
    healthcheck:
      command: ["curl", "--fail", "--silent", "--show-error", "http://127.0.0.1:32201/healthz"]
      interval_ms: 400
      timeout_ms: 300
      retries: 20
  message-worker:
    runtime: host
    command: ["python3", "scripts/run-sibling.py", "run", "message-worker"]
    build:
      - ["python3", "scripts/run-sibling.py", "build", "message-worker"]
    depends_on: ["db-bootstrap", "presence-service"]
    healthcheck:
      command: ["curl", "--fail", "--silent", "--show-error", "http://127.0.0.1:32202/healthz"]
      interval_ms: 400
      timeout_ms: 300
      retries: 20
  workspace-web:
    runtime: host
    command: ["python3", "scripts/run-sibling.py", "run", "workspace-web"]
    build:
      - ["python3", "scripts/run-sibling.py", "build", "workspace-web"]
    depends_on: ["db-bootstrap", "presence-service", "message-worker"]
    healthcheck:
      command: ["curl", "--fail", "--silent", "--show-error", "http://127.0.0.1:32203/healthz"]
      interval_ms: 400
      timeout_ms: 300
      retries: 20
  app:
    runtime: host
    command: ["bash", "scripts/dev-server.sh"]
    depends_on: ["db-bootstrap", "presence-service", "message-worker", "workspace-web"]
    healthcheck:
      command: ["curl", "--fail", "--silent", "--show-error", "http://127.0.0.1:3110/"]
      interval_ms: 500
      timeout_ms: 400
      retries: 20
