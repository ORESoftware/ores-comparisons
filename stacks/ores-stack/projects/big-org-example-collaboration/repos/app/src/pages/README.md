# Page tree

Add MASH/Leptos/Dioxus pages here when measuring SSR/hydration. Browser components should consume `ores-forms`, `opto-sync`, `ores-chat`, and `ores-convo` through generated/shared interfaces while retaining the same `api-docs` route identities as the native server.
