# ORES Stack rpc-graphql

Standalone Rust server version of the matched RPC/GraphQL workload. `api-docs` remains authoritative for operation identity and the route-map digest consumed by `ores-stack`.

```sh
ores-stack check
ores-stack routes
ores-stack build --no-exec
cargo check
```
