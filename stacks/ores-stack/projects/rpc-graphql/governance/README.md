# ORES Stack RPC/GraphQL governance

ORES Stack generates native Rust/WASM/RPC/GraphQL surfaces from the common resource/event authority. Transport generators cannot renumber Protobuf fields or redefine persistence types.
