# Page tree

Add MASH/Leptos/Dioxus operations consoles here when measuring browser-WASM and SSR costs. Keep page data sources bound to the same `api-docs` operations and shared middleware context used by the standalone server.
