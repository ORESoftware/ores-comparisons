# ORES Stack form-service

Standalone Rust server version of the matched form workload. Use `ores-stack` for deterministic contract admission/builds and Cargo for the server binary.

```sh
ores-stack check
ores-stack routes
ores-stack build --no-exec
cargo check
```
