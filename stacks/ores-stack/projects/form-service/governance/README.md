# ORES Stack form-service governance

Rust/WASM/server generation consumes the shared form authority. ORES Stack may generate HTTP/RPC/GraphQL/page bindings but may not replace TypeSpec or JSON Schema as the entity authority.
