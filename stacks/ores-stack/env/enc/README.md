# ORES Stack encrypted environments

Commit only SOPS ciphertext here. Encrypt dotenv/YAML inputs with organization age recipients and decrypt into `../dec/` only for local execution or an isolated CI job.
