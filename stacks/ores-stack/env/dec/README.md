# ORES Stack decrypted environments

Local-only SOPS output. Git ignores environment material in this directory. Server, RPC, GraphQL, cache, sync, chat, and telemetry credentials are injected from the environment rather than authored into `.ores-stack.toml` or Rust source.
