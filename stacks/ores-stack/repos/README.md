# Optional source checkouts

Reserve this directory for pinned git submodules representing the ORES Stack organization when source-level comparison is required. Prefer the migrated `ores-stack/*` repositories (`ores-stack-cli`, `ores-stack-lib-core`, `ores-stack-pub-lib-core`, `ores-stack-interfaces`, `ores-stack-infra`) over the legacy monolithic `ORESoftware/ores-stack` checkout.
